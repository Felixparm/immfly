export enum PriceCategory {
  RETAIL = 'retail',
  CABINE_CREW = 'cabine_crew',
  HAPPY_HOUR = 'happy_hour',
  BUSINESS_INVITATION = 'business_invitation',
  TOURISM_INVITATION = 'tourism_invitation',
}

export const PRICE_CATEGORY_OPTIONS = Object.values(PriceCategory);